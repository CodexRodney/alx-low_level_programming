#include "main.h"
#include <stdio.h>

/**
 * _abs- Computes the absolute value of a number
 * 
 * Return: Always absolute value of a number
 */

int _abs(int)
{
	int _num(a)
	{
		if (a < 0)
			return (-a);
		else
			return (a);
	}
	return (_num(a));
}
